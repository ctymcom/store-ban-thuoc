export const ROLES = {
  ADMIN: "ADMIN",
  EDITOR: "EDITOR",
};

export enum SettingTypeEnum {
  string = "string",
  number = "number",
  array = "array",
  object = "object",
}

export enum EventErrorStatusEnum {
  error = "error",
  resolved = "resolved",
}
