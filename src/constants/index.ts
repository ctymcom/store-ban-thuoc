export const ROLES = {
  ADMIN: "ADMIN",
  EDITOR: "EDITOR",
};

export enum SettingTypeEnum {
  string = "string",
  number = "number",
  array = "array",
  object = "object",
}

export enum EventErrorStatusEnum {
  error = "error",
  resolved = "resolved",
}

export enum EventErrorTypeEnum {
  example_1 = "example_1",
  example_2 = "example_2",
}
